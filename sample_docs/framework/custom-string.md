# Custom string API

`DemoString` is the framework-owned string type used by this sample documentation.

Before using it, verify its real constructor, conversion, encoding and ownership rules in the framework API reference. Do not infer methods from a similar standard-library type.
