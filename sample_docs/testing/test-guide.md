# Testing guide

Every MCP tool must have an end-to-end protocol test that covers initialization, discovery, invocation and error behavior.

Tests should use a temporary or sample documentation directory and must not modify production documentation.
