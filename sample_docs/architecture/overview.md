# Architecture

Business logic and transport adapters must remain separate. MCP handlers adapt parameters; independently testable classes own the core behavior.

The documentation repository is read-only. Any file-writing capability requires a separate permission and human-confirmation design.
